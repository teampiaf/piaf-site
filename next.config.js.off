/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // On ne met PAS de basePath, ça force le site à la racine
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;